model Team {
  id            Int            @id @default(autoincrement())
  name          String         @unique
  teamLead      Int            @unique
  status        Int            @default(0) 
  category      String
  paymentID     String?
  paymentStatus Int            @default(0)
  paymentProof  Int
  submissions   Int[]
}